# 02 — LLM call: structured web extraction

_Not captured by the in-process trace: the web-extraction call is issued by the native web-search backend via the provider's raw client, not through the assistant's `chat()` proxy. The raw inputs/outputs are in files 01 and 03._
